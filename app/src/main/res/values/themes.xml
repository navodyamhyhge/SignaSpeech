<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.SignLanguageTranslator" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">#1E3A8A</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Add this if you want to keep using Theme.App -->
    <style name="Theme.App" parent="Theme.SignLanguageTranslator" />
</resources>